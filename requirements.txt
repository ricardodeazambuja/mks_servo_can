# This file is not the source of truth for dependencies. pyproject.toml is.
#
# Everything below is declared there, where pip can act on it and where the
# extras say which dependency is needed for what:
#
#   pip install .              library only
#   pip install .[simulator]   + the mks-servo-simulator command and its debug API
#   pip install .[dashboard]   + textual, for the legacy --textual-dashboard TUI
#   pip install .[monitoring]  + psutil, for the performance-monitoring panels
#   pip install -e .[dev]      all of the above + pytest and ruff
#
# This file is kept only so that `pip install -r requirements.txt` still does
# something sensible for anyone following older instructions. It installs the
# equivalent of `.[simulator,monitoring]` plus the test runners, but it does not
# install the project itself - you still need one of the commands above.
#
# A duplicated dependency list is a list that will disagree with the real one.
# If you are editing this file, edit pyproject.toml instead.

python-can>=3.0.0,<5.0.0

click>=7.0
rich>=12.0.0
fastapi>=0.68.0
uvicorn>=0.15.0
psutil>=5.8.0

pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-mock>=3.0.0
