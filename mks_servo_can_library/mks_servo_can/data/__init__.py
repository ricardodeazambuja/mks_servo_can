"""
Package data shipped with the library.

Currently the machine-readable transcription of the MKS SERVO42D/57D CAN manual,
read through `mks_servo_can.manual_spec`. It lives in a package rather than in
`tests/fixtures/` so that an installed wheel carries it; see MANUAL_SPEC_NOT_PACKAGED
(L7) in REVIEW_NOTES.md.
"""
